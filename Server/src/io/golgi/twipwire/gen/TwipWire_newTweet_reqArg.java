/* IS_AUTOGENERATED_SO_ALLOW_AUTODELETE=YES */
/* The previous line is to allow auto deletion */

package io.golgi.twipwire.gen;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Iterator;
import com.openmindnetworks.golgi.JavaType;
import com.openmindnetworks.golgi.GolgiPayload;
import com.openmindnetworks.golgi.B64;
import com.openmindnetworks.golgi.api.GolgiException;
import com.openmindnetworks.golgi.api.GolgiAPI;

public class TwipWire_newTweet_reqArg
{

    private boolean corrupt = false;

    public boolean isCorrupt() {
        return corrupt;
    }

    public void setCorrupt() {
        corrupt = true;
    }

    private boolean tweetDetailsIsSet = false;
    private TweetDetails tweetDetails;

    public TweetDetails getTweetDetails(){
        return tweetDetails;
    }
    public boolean tweetDetailsIsSet(){
        return tweetDetailsIsSet;
    }
    public void setTweetDetails(TweetDetails tweetDetails){
        this.tweetDetails = tweetDetails;
        this.tweetDetailsIsSet = true;
    }

    public StringBuffer serialise(){
        return serialise(null);
    }

    public StringBuffer serialise(StringBuffer sb){
        return serialise("", sb);
    }

    public StringBuffer serialise(String prefix, StringBuffer sb){
        if(sb == null){
            sb = new StringBuffer();
        }

        if(this.tweetDetailsIsSet){
            tweetDetails.serialise(prefix + "" + 1 + "." , sb);
        }

        return sb;
    }

    private void deserialise(GolgiPayload payload){
        if(!isCorrupt() && payload.containsNestedKey("1")){
            TweetDetails inst = new TweetDetails(payload.getNested("1"));
            setTweetDetails(inst);
               if(inst == null || inst.isCorrupt()){
                   setCorrupt();
               }
        }
        else {
            setCorrupt();
        }
    }

    public TwipWire_newTweet_reqArg(){
        this(true);
    }

    public TwipWire_newTweet_reqArg(boolean isSetDefault){
        super();
        tweetDetailsIsSet = isSetDefault;
        tweetDetails = new TweetDetails(isSetDefault);
    }

    public TwipWire_newTweet_reqArg(GolgiPayload payload){
        this(false);
        deserialise(payload);
    }

    public TwipWire_newTweet_reqArg(String payload){
        this(JavaType.createPayload(payload));
    }

}
